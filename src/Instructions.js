import React from 'react';

const Instructions = props => {

  return(
    <div>
      <p>This tool lets you seperate Amazon FBA Shipping labels in order for them to be compatiable with any label printer.</p>
      <p>Follow the steps below to convert the label provided by Amazon.</p>
    </div>
  );
}

export default Instructions;